name: CRM CI

on:
  push:
    branches:
      - feature/crm-recompra
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: CRM unit tests
        run: bunx vitest run src/lib/crm-rfm.test.ts src/lib/crm-repurchase-shared.test.ts src/lib/crm-segmentation-shared.test.ts

      - name: Build
        run: bun run build

      - name: Typecheck
        run: bunx tsc --noEmit
